---
- hosts: wireguard-mesh
  vars_files:
    - secrets.yml
  vars:
    sysctl_settings:
      - { name: net.ipv4.ip_forward, value: "1" }
  pre_tasks:
    - include_tasks: tasks/sysctl.yml
  roles:
    - wireguard
